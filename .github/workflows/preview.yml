# DISABLED — Vercel PR preview deployment replaced by Render.
# Kept for reference. All jobs are disabled with if: false.
name: Preview (Vercel — disabled)

on:
  pull_request:
    branches: [main, develop]
    types: [opened, synchronize, reopened]

concurrency:
  group: preview-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  preview:
    name: Deploy PR preview — DISABLED
    runs-on: ubuntu-latest
    if: false   # Disabled: project moved to Render
    permissions:
      contents: read
      pull-requests: write
    steps:
      - run: echo "Vercel preview disabled — using Render"

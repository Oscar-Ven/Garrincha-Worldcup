# DISABLED — Vercel staging deployment replaced by Render.
# Kept for reference. All jobs are disabled with if: false.
name: Deploy Staging (Vercel — disabled)

on:
  workflow_run:
    workflows: ["CI"]
    branches: [develop]
    types: [completed]

concurrency:
  group: deploy-staging
  cancel-in-progress: false

jobs:
  deploy-staging:
    name: Deploy to Vercel (staging) — DISABLED
    runs-on: ubuntu-latest
    if: false   # Disabled: project moved to Render
    steps:
      - run: echo "Vercel staging deploy disabled — using Render"

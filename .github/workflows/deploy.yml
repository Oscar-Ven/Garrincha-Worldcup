# DISABLED — Vercel deployment replaced by Render.
# Kept for reference. All jobs are disabled with if: false.
# Remove this file once Render deployment is confirmed stable.
name: Deploy (Vercel — disabled)

on:
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types: [completed]

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Vercel (production) — DISABLED
    runs-on: ubuntu-latest
    if: false   # Disabled: project moved to Render
    steps:
      - run: echo "Vercel deploy disabled — using Render"

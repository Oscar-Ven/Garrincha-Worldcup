name: Deploy (Render Worker Disabled)

# Render is reserved for a future background worker / football API sync.
# The main Next.js app deploys to Vercel.
on:
  workflow_dispatch:

jobs:
  disabled:
    name: Render worker deploy not configured
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Render worker deployment is not configured yet."
